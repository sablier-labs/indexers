import "../envio.just"

export ENVIO_HASURA_PUBLIC_AGGREGATE := ```
    arr=(
        "Action"
        "Asset"
        "Batch"
        "Stream"
    )
    IFS='&' # Internal Field Separator
    echo "${arr[*]}"
```
export ENVIO_INDEXER_NAME := "flow"
export INDEXER_NAME := "flow"

# Show available commands
default:
    @just --list
